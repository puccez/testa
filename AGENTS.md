# Expo HAS CHANGED

Read the exact versioned docs at <https://docs.expo.dev/versions/v56.0.0/> before writing any code.

Non usare mai il web per vedere le modifiche che stai facendo, utilizza sempre l'expo mcp per screenshot e computer use (se lo hai) per muoverti
